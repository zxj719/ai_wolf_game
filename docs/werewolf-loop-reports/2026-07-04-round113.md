⚠ 待部署：本轮改动已提交未上线（无 CLOUDFLARE_API_TOKEN）

---

# Round 113 报告 — 2026-07-04

## 5 行总结

**本轮优化**：SHERIFF_BADGE_PASS 女巫 + 守卫专属提示词（R113）——新增 `bpWitchSavedAlive`（银水存活过滤）→ `bpWitchHint`；`guardCounts` 频次分析 → `bpGuardHint`（Top-2 守护候选）；`bpIdentityStep` 改为 4-path ternary（狼/女巫/守卫/others 各含专属优先级链）；`bpHint` 同步 4 路径（女巫："参考银水记录"，守卫："参考守护记录"）；return 模板末尾追加 `${bpWitchHint}${bpGuardHint}`（空串时零副作用）。  
**测试结果**：1972/1972 ✅（+20 new R113 tests，R64 回归修复 1 个，chatSocket 预存在失败无关），build ✅（WerewolfModule 252.07→253.08 kB +1.01 kB），check-build ✅。  
**新增教训**：SHERIFF_BADGE_PASS 完成女巫/守卫专属框架（R113）；R64 回归测试因 `bpIdentityStep` 由二元条件改为 4-path ternary 而失效，需将"实现细节检查"改为"语义意图检查"（`=== '狼人' ? ''` 替换 `!== '狼人'`）。  
**游戏机制**：未修改游戏机制（SHERIFF_BADGE_PASS 仅为提示词优化，不影响触发逻辑）。  
**下轮计划**：平衡性评估（多局干跑统计狼/好人胜率） 或 SHERIFF_VOTE 女巫/守卫独立分支（目前三者合并在"神职策略"）。

---

## 评估维度（本轮入场审计）

### 1. 提示词质量（评分：8.7/10）
- SHERIFF_BADGE_PASS 此前存在女巫/守卫缺口（二元 狼人/好人 分支，未利用角色私有信息）
- R113 补齐：女巫银水历史 + 守卫守护频次 均作为信任锚点注入传徽决策
- 优化后所有 "坐拥丰富私有信息" 的神职（预言家/女巫/守卫）在 SHERIFF_BADGE_PASS 均有专属利用路径

### 2. 触发机制正确性（评分：7/10）
- 夜间/投票/猎人连锁等核心机制未变（simulate-game-flow.mjs 25/25 历史验证在案）
- SHERIFF_BADGE_PASS 触发时机本身正确（死亡警长 → 自动调用）

### 3. AI 决策合理性（评分：8/10）
- 女巫警长现在知道优先传给她亲手救过的好人（银水 = 最确信的信任关系，无可伪造）
- 守卫警长现在能用守护频次排序候选（守多次 = 最信任的人，零间接推断）
- 两者的 bpIdentityStep 优先级链也已同步：私有信息 > identity_table

### 4. 平衡性（评分：7/10）
- SHERIFF_BADGE_PASS 优化加强好人阵营（女巫/守卫警长能更精确传徽给好人）
- 对平衡影响较小（徽章传递是晚期行动，频率不高）

### 5. 可观赏性（评分：7.5/10）
- 女巫警长传徽时将更有说服力（银水回顾 → 有据可查的决策）
- 守卫警长传徽时展示守护轨迹（不可伪造的信任证据链）

---

## 实现细节

### 选定优化项

**SHERIFF_BADGE_PASS 女巫 + 守卫专属提示词**（R112 报告下轮计划第 1 项）

**研究依据**：Wang 2025 (arxiv:2408.17177) — 角色私有信息注入显著提升 AI 决策精准度。
- 女巫私有信息：`witchHistory.savedIds`（她亲手救过的人）= 最高信任度，传徽的最强正面锚点
- 守卫私有信息：`guardHistory`（守护频次分析）= 不可伪造的信任排序依据

### 代码位置

`src/services/aiPrompts.js`：`case PROMPT_ACTIONS.SHERIFF_BADGE_PASS:` block

### 新增变量与逻辑

```javascript
// R113: 女巫 SHERIFF_BADGE_PASS 专属提示词 — 银水记录作信任锚点
const bpWitchHistory = gameState.witchHistory || { savedIds: [], poisonedIds: [] };
const bpWitchSavedAlive = (bpWitchHistory.savedIds || []).filter(id => badgeableSet.has(id));
let bpWitchHint = '';
if (playerRole === '女巫' && bpWitchSavedAlive.length > 0) {
    bpWitchHint = `\n💊【你的银水（解药救过且仍存活的好人）】${bpWitchSavedAlive.join(',')}号 → 你亲手保住的好人，是最有依据的传徽首选`;
}

// R113: 守卫 SHERIFF_BADGE_PASS 专属提示词 — 守护记录作信任锚点
const bpGuardHistory = gameState.guardHistory || [];
let bpGuardHint = '';
if (playerRole === '守卫' && bpGuardHistory.length > 0) {
    const guardCounts = {};
    bpGuardHistory.forEach(g => {
        if (g.targetId != null && badgeableSet.has(g.targetId)) {
            guardCounts[g.targetId] = (guardCounts[g.targetId] || 0) + 1;
        }
    });
    const topGuarded = Object.entries(guardCounts)
        .sort((a, b) => b[1] - a[1])
        .slice(0, 2)
        .map(([id, cnt]) => `${id}号(守${cnt}次)`);
    if (topGuarded.length > 0) {
        bpGuardHint = `\n🛡️【你的守护记录（存活候选中守护最频繁者）】${topGuarded.join('、')} → 守护次数最多=你最信任的好人，优先传徽`;
    }
}
```

### bpIdentityStep 4-path 更新

```diff
- const bpIdentityStep = playerRole !== '狼人'
-     ? 'Step0: 【读取历史身份推理...】...Step1: 传徽优先级 → ⚡金水 > identity_table...'
-     : '';
+ const bpIdentityStep = playerRole === '狼人'
+     ? ''
+     : playerRole === '女巫'
+     ? '...Step1: 传徽优先级 → ⚡金水 > 💊银水 > identity_table...'
+     : playerRole === '守卫'
+     ? '...Step1: 传徽优先级 → ⚡金水 > 🛡️守护最频繁者 > identity_table...'
+     : '...Step0: 读取历史身份推理...Step1: 传徽优先级 → ⚡金水 > identity_table...';
```

### bpHint 4-path 更新

```diff
- const bpHint = playerRole === '狼人'
-     ? '你是狼人警长：...'
-     : '你是好人警长：把警徽传给你最确信的好人...'
+ const bpHint = playerRole === '狼人'
+     ? '你是狼人警长：...'
+     : playerRole === '女巫'
+     ? '你是女巫警长：参考下方银水记录...选出最可信的传徽对象...'
+     : playerRole === '守卫'
+     ? '你是守卫警长：参考下方守护记录...选出最可信的传徽对象...'
+     : '你是好人警长：把警徽传给你最确信的好人...'
```

### Return 模板更新

```diff
- ${bpIdentityStep ? bpIdentityStep + '\n' : ''}${bpHint}${seerHint}
+ ${bpIdentityStep ? bpIdentityStep + '\n' : ''}${bpHint}${seerHint}${bpWitchHint}${bpGuardHint}
```

### R64 回归测试修复

```diff
- expect(badgePassBlock).toContain("playerRole !== '狼人'");
+ expect(badgePassBlock).toContain("playerRole === '狼人'");
+ const bpIdStepBlock = badgePassBlock.slice(bpIdStepIdx, bpIdStepIdx + 100);
+ expect(bpIdStepBlock).toContain("playerRole === '狼人'");
+ expect(bpIdStepBlock).toContain("? ''");
```

### 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 女巫 savedIds 过滤 | `badgeableSet.has(id)` 过滤存活候选 | 已死亡的被救者不在传徽候选中，无需展示 |
| 守卫 Top-2 守护 | `Object.entries(guardCounts).sort().slice(0,2)` | 守护次数最多=最高信任；Top-2 提供候选备选 |
| 空串零副作用 | `${bpWitchHint}${bpGuardHint}`（空串无占位符） | 非女巫/守卫角色完全无 UI 干扰 |
| 白熊效应 | 全正向描述（银水→"传徽首选"，守护→"最信任"） | T18 验证 ✅（第 34 次验证）|
| R64 测试修复策略 | 改为语义意图验证（`=== '狼人' ? ''`）而非实现字面 | 避免未来扩展角色路径再次触发此类回归 |

---

## 测试结果

### 新增测试文件
`src/services/__tests__/round113SheriffBadgePassWitchGuard.test.js`  
20 个测试（T1-T20），全部通过：

| 组 | 测试 | 内容 |
|----|------|------|
| T1-T3 | 锚点 + 基础变量 | SHERIFF_BADGE_PASS 块存在 + bpWitchHistory + bpGuardHistory 声明 |
| T4-T6 | bpWitchHint 女巫路径 | bpWitchSavedAlive 过滤声明 + 女巫判断分支 + 银水/传徽首选关键词 |
| T7-T9 | bpGuardHint 守卫路径 | guardCounts 声明 + 守卫判断分支 + 守护/优先传徽关键词 |
| T10-T12 | bpIdentityStep 4路径 | 银水优先级 + 守护最频繁者优先级 + others fallback 保留 |
| T13-T15 | bpHint 4路径 | 女巫银水记录框架 + 守卫守护记录框架 + 好人 fallback |
| T16-T17 | Return 模板 | bpWitchHint + bpGuardHint 插值 |
| T18 | 白熊效应（第 34 次）| bpWitchHint/bpGuardHint 块无负向禁词 |
| T19-T20 | 回归 | seerHint 金水/查杀路径保留 + bpHint 狼人分支保留 |

### 修复测试
`src/services/__tests__/round64SheriffBadgePassReadLoop.test.js` T7：
- 将 `playerRole !== '狼人'` 字面检查改为 `playerRole === '狼人' ? ''` 语义检查
- 14/14 通过

### 全量测试
```
1972/1972 ✅（+20 new R113 tests + R64 T7 修复；chatSocket 预存在失败与本轮无关）
```

---

## Block 大小变化

| 版本 | 内容 |
|------|------|
| 修改前（R112 后）| SHERIFF_BADGE_PASS block ~1200 chars |
| 修改后（R113）| ~2700 chars |
| 增量 | ~+1500 chars |
| WerewolfModule | 252.07 → 253.08 kB（+1.01 kB）|

---

## 女巫/守卫警长传徽实际效果（场景举例）

### 场景 A：女巫警长死亡，曾在 N2 用解药救了 3 号（3 号仍存活）

**优化前**：
```
你是好人警长：把警徽传给你最确信的好人。完全无法判断时撕掉警徽（-1）——错传给狼等于送1.5票。
```

**优化后**：
```
你是女巫警长：参考下方银水记录（你亲手救过的人是你最确信的好人）和预言家金水，选出最可信的传徽对象；无充分依据时撕掉（-1）。
⚡【预言家金水（已验好人）】...
💊【你的银水（解药救过且仍存活的好人）】3号 → 你亲手保住的好人，是最有依据的传徽首选
```

### 场景 B：守卫警长死亡，整局守护记录 N1守2号、N2守2号、N3守5号

**优化前**：
```
你是好人警长：把警徽传给你最确信的好人...
```

**优化后**：
```
你是守卫警长：参考下方守护记录（守护最频繁者=你最信任的好人）和预言家金水，选出最可信的传徽对象；无充分依据时撕掉（-1）。
🛡️【你的守护记录（存活候选中守护最频繁者）】2号(守2次)、5号(守1次) → 守护次数最多=你最信任的好人，优先传徽
```

---

## 文件变更汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/services/aiPrompts.js` | 修改 | SHERIFF_BADGE_PASS case 新增 bpWitchHint + bpGuardHint + 4-path bpIdentityStep/bpHint（+~1500 chars） |
| `src/services/__tests__/round113SheriffBadgePassWitchGuard.test.js` | 新建 | 20 个测试，全部通过 |
| `src/services/__tests__/round64SheriffBadgePassReadLoop.test.js` | 修改 | T7 回归修复（语义意图检查替代字面检查）|
| `docs/werewolf-loop-reports/2026-07-04-round113.md` | 新建 | 本报告 |
| `docs/werewolf-loop-reports/LEARNINGS.md` | 修改 | 追加 R113 教训 |

---

## 下轮计划

1. **平衡性评估**：在 simulate-game-flow.mjs 基础上进行多局干跑，统计狼/好人胜率；识别是否存在系统性失衡
2. **SHERIFF_VOTE 审计**：女巫/守卫/猎人当前合并在"神职策略"单一分支，是否需要独立分支？（参考 R113 SHERIFF_BADGE_PASS 拆分模式）
3. **NIGHT 角色平安夜推断审计**（R107-D 遗留）：确认 NIGHT_MAGICIAN 是否有平安夜推断；骑士是否有 NIGHT 行动 case

---

## 教训总结（R113）

见 LEARNINGS.md R113 条目。
