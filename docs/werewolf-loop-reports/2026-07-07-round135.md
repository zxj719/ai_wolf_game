# Round 135 — SHERIFF_SPEECH 骑士竞选对跳检测：ssKnightCounterClaimants + knightSsCounterHint

⚠ 待部署：本轮改动已提交推送至 main，但无 CLOUDFLARE_API_TOKEN，未执行 `npm run deploy`。

**日期**：2026-07-07  
**耗时**：~20 min

---

## 本轮摘要（5 行）

- **优化**：SHERIFF_SPEECH 骑士竞选对跳检测全闭环：① aiPrompts.js 新增 `ssKnightCounterClaimants`/`knightSsCounterHint` 变量（骑士门控，过滤 `jump_knight` 排除自身，有对跳时注入底气框架提示）；② 骑士 ssHint 分支从 `knightSsHint` 改为 `` `${knightSsHint}${knightSsCounterHint}` ``
- **结构特点**：与 R134 预言家检测完全对称（ssCounterClaimants → ssKnightCounterClaimants，jump_seer → jump_knight），无需修改 useDayFlow.js/WerewolfModule.jsx（claimHistory 写入已由 R134 完成）
- **SS_WINDOW 升级**：block 6750→7334（+584 chars），余量 166 < 500 → 按铁律升级至 8500（余量 1166 ✅）；round112 + round134 test 文件 SS_WINDOW 同步 7500→8500
- **测试**：2429/2429 通过（+11 新增 R135 tests T1-T11）；干跑 25/25 ✅
- **下轮建议**：平衡性调整（Options A/B/C 待用户拍板）；或 SHERIFF_SPEECH 狼人悍跳骑士时 `claims` 字段指引

---

## 五维评估

| 维度 | 分数 | 备注 |
|------|------|------|
| 提示词质量 | 9/10 | SHERIFF_SPEECH 竞选对跳闭环：预言家（R134）+ 骑士（R135）均已完成 |
| 游戏触发机制 | 9/10 | 骑士竞选发言时可检测到先行声称骑士的对手，底气框架引导精准 |
| AI 决策合理性 | 8/10 | 骑士对跳时提供决斗锚点引导（已用决斗者可报目标），未决斗者用主动魄力框架 |
| 平衡性 | ⚠ 6/10 | 狼胜率 84-90%（Options A/B/C 待决策，未变更） |
| 可观战性 | 8/10 | 骑士竞选发言在对跳场景中更具对抗性 |

---

## 问题分析

### 骑士 SHERIFF_SPEECH 对跳盲点

与 R134 预言家场景对称：

```
Wolf（候选人A） → SHERIFF_SPEECH → 声称"我是骑士，已对X号发起/准备决斗"
            ↓
  R134 修复：res.claims → recordClaim({type:'jump_knight'}) ← 注意：仅当狼人填 claims 时写入
            ↓
Knight（候选人B）→ SHERIFF_SPEECH → 无 ssKnightCounterClaimants → 无对跳感知 ❌（R134之前）
                                  ↓
                  R135修复后 → ssKnightCounterClaimants 非空 → knightSsCounterHint 注入 ✅
```

注：jump_knight 写入 claimHistory 依赖狼人在输出 JSON 的 claims 字段填写 `{type:'jump_knight',...}`。目前 wolf ssHint ① 只有 jump_seer 的 claims 指引（R134），jump_knight 指引为下轮可选优化。

---

## 实现细节

### 1. aiPrompts.js — SHERIFF_SPEECH block

在 `seerSsCounterHint` 之后、`const ssHint` 之前新增 2 个变量：

```javascript
// R135: 骑士竞选对跳检测——对称 R134 预言家逻辑
const ssKnightCounterClaimants = playerRole === '骑士'
    ? (gameState.claimHistory || [])
        .filter(c => c.type === 'jump_knight' && c.playerId !== currentPlayer?.id)
        .map(c => c.playerId)
    : [];
const knightSsCounterHint = ssKnightCounterClaimants.length > 0
    ? `\n⚡【对跳情境】${ssKnightCounterClaimants.join('/')}号已在竞选中声称是骑士——核心反制：用底气压制，若已使用决斗可报目标作信任锚点；未决斗则以"主动魄力"框架坚守，无需争论身份真假。`
    : '';
```

骑士 ssHint 分支修改：

```javascript
// 修改前：
: playerRole === '骑士'
? knightSsHint

// 修改后：
: playerRole === '骑士'
? `${knightSsHint}${knightSsCounterHint}`
```

### 2. SS_WINDOW 升级

| 指标 | R134 | R135 |
|------|------|------|
| SHERIFF_SPEECH block | 6750 | 7334 |
| SS_WINDOW | 7500 | 8500 |
| 余量 | 750 | 1166 |
| 安全线 500 | ✅ | ✅ |

---

## 测试结果

| 指标 | 值 |
|------|----|
| 总测试数 | 2429/2429 ✅ |
| 新增测试 | 11（R135 T1-T11 全通过） |
| round112 SS_WINDOW | 7500→8500 ✅ |
| round134 SS_WINDOW | 7500→8500 ✅ |
| 干跑 | 25/25 ✅ |
| Build | ✅（0 localhost 泄露） |
| WerewolfModule | 265.19 kB（+0.25 kB，符合预期） |
| SS 余量 | 1166 chars（窗口 8500 - block 7334，安全 ✅） |

---

## 重要教训（→ LEARNINGS.md）

1. **R135-A**：骑士 SHERIFF_SPEECH 对跳检测对称完成 — `ssKnightCounterClaimants` + `knightSsCounterHint` 追加到骑士 ssHint 分支，与 R134 预言家逻辑完全对称。无需修改 useDayFlow.js（claimHistory 写入已由 R134 完成）。
2. **R135-B**：SS_WINDOW 7500→8500 — R135 新增代码使 block 从 6750 → 7334 chars，7500 窗口余量降至 166（< 500 安全线），按铁律升级。两个已有 test 文件（round112/round134）SS_WINDOW 同步更新。

---

## 下轮建议

**优先 A（需用户决策）**：平衡性调整（Options A/B/C pending，狼胜率 84-90% vs 目标 40-60%）

**优先 B（可直接执行）**：
- SHERIFF_SPEECH 狼人悍跳骑士时 `claims` 字段指引（wolf ssHint 追加 ② 项：`{type:'jump_knight',duel:{targetId:X}}`），与预言家悍跳 ① 项对称
- 或 SHERIFF_SPEECH 狼人双路悍跳协调提示（多只狼参选时，避免两只狼同时跳预言家）
