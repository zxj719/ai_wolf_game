# Round 137 — SHERIFF_SPEECH 多狼竞选协调（ssWolfCoordHint 分工策略）

⚠ 待部署：本轮改动已提交推送至 main，但无 CLOUDFLARE_API_TOKEN，未执行 `npm run deploy`。

**日期**：2026-07-08  
**耗时**：~25 min

---

## 本轮摘要（5 行）

- **优化**：SHERIFF_SPEECH 新增多狼竞选协调机制：`ssOtherWolfCandidates`（检测其他狼候选）+ `ssWolfClaimedSeer`（检测已悍跳预言家的队友）+ `ssWolfClaimedKnight`（检测已悍跳骑士的队友）+ `ssWolfCoordHint`（有已跳队友时注入路径③分工建议），append 到 wolf ssHint ③ 路径末尾（零副作用）
- **修复意义**：两只狼同时悍跳同一身份会产生高度内聚的身份信号，好人能识别身份关联；本轮引导后发言的狼人感知队友已跳身份，动态切换为好人竞选分析，分散信息层次
- **测试**：2465/2465 通过（+12 tests T1-T12 R137；干跑 25/25 ✅；build ✅，WerewolfModule 266.00 kB，check-build ✅，0 localhost 泄露）
- **新增教训**：SS_WINDOW 8500→9500（block 8894 > 8500，余量 606 > 500 ✅）；凡新增 SHERIFF_SPEECH 代码必须运行 block-size 检测命令；多狼场景协调应检测 claimHistory 而非靠 AI 自主协调
- **下轮计划**：平衡性调整（Options A/B/C 待用户拍板）；或 LAST_WORDS 骑士遗言（骑士发现悍跳对手时遗言揭示）

---

## 五维评估

| 维度 | 分数 | 备注 |
|------|------|------|
| 提示词质量 | 9/10 | wolf ssHint 三路径 + 多狼协调分工；协调基于 claimHistory 结构数据而非 NLP 推断 |
| 游戏触发机制 | 9/10 | 多狼协调读 claimHistory（由 R134 recordClaim 写入），与现有闭环完全对称 |
| AI 决策合理性 | 8/10 | 后发狼人获得队友声明信息，能动态调整竞选策略（分工最优） |
| 平衡性 | ⚠ 6/10 | 狼胜率 84-90%（pending 用户决策 Options A/B/C） |
| 可观战性 | 8/10 | 两狼协调时出现明显的角色分工：一个信息框架者 + 一个分析者，更丰富的观战层次 |

---

## 问题分析

### 多狼竞选身份信号内聚问题（R137 修复）

**问题**：在 R134-R136 实现 jump_seer/jump_knight claimHistory 写入之后，存在一个系统性问题：
当 2 只狼人同时是警长候选人时，各自独立的 wolf ssHint 只有三条静态路径（①悍跳预言家 ②悍跳骑士 ③好人竞选），没有相互感知机制。

**后果**：两只狼人独立决策，概率上可能各自都选择路径①（悍跳预言家），产生：
1. 两人发言结构相同（"夜1查验了X号，金水/查杀"）
2. 好人能通过结构相似性识别身份关联
3. 真预言家有两个对跳目标时更有说服力（反而让真预言家受益）

**修复**：
```
检测链：claimHistory → 其他狼候选已跳身份 → ssWolfCoordHint 注入路径③建议
                    ↑
         R134 recordClaim 写入（竞选循环结束后解析 res.claims 调用）
```

R137 完成后，后发言的狼人会看到队友的已声明身份，动态切换为好人竞选分析角度。

---

## 实现细节

### 1. aiPrompts.js — 新增 4 个变量（lines 2976-2997，在 knightSsCounterHint 之后、ssHint 之前）

```javascript
// R137: 多狼竞选协调——检测其他狼队友的已有声明，指导分工策略
const ssOtherWolfCandidates = playerRole === '狼人'
    ? (candidateIds || []).filter(id =>
        String(id) !== String(currentPlayer?.id) &&
        players.find(p => String(p.id) === String(id) && p.role === '狼人'))
    : [];
const ssWolfClaimedSeer = ssOtherWolfCandidates.length > 0
    ? (gameState.claimHistory || [])
        .filter(c => c.type === 'jump_seer' && ssOtherWolfCandidates.some(id => String(c.playerId) === String(id)))
        .map(c => c.playerId)
    : [];
const ssWolfClaimedKnight = ssOtherWolfCandidates.length > 0
    ? (gameState.claimHistory || [])
        .filter(c => c.type === 'jump_knight' && ssOtherWolfCandidates.some(id => String(c.playerId) === String(id)))
        .map(c => c.playerId)
    : [];
const ssWolfCoordHint = ssOtherWolfCandidates.length > 0 && (ssWolfClaimedSeer.length > 0 || ssWolfClaimedKnight.length > 0)
    ? `\n【队友信息】${[...ssWolfClaimedSeer.map(id => `${id}号队友已走路径①（悍跳预言家）`), ...ssWolfClaimedKnight.map(id => `${id}号队友已走路径②（悍跳骑士）`)].join('；')}——两人取不同角度最优：路径③（好人竞选）是当前最佳选择，双角色多元覆盖让好人更难识别身份关联。claims 留 []`
    : '';
```

### 2. aiPrompts.js — wolf ssHint ③ 路径末尾追加（line 3003）

**修改前：**
```
③ 若以好人身份竞选：展示分析能力，说出你对昨夜局势的判断和票型倾向——暗示你当选后能主导好人找出"狼人"（实际是在控制信息流）；claims 留 []`
```

**修改后：**
```
③ 若以好人身份竞选：展示分析能力，说出你对昨夜局势的判断和票型倾向——暗示你当选后能主导好人找出"狼人"（实际是在控制信息流）；claims 留 []${ssWolfCoordHint}`
```

### 3. 4 个测试文件 SS_WINDOW 升级（8500 → 9500）

- `round112SheriffSpeechWitchGuard.test.js`
- `round134SheriffSpeechCounterClaim.test.js`
- `round135SheriffSpeechKnightCounterClaim.test.js`
- `round136SheriffSpeechWolfJumpKnight.test.js`

---

## 测试结果

| 指标 | 值 |
|------|----|
| 总测试数 | 2465/2465 ✅ |
| 新增测试 | 12（R137 T1-T12 全通过） |
| SS block 大小 | 8894 chars（R136: 7520, +1374） |
| SS_WINDOW | 9500（升级自 8500） |
| SS 余量 | 606 chars（> 500 安全线 ✅） |
| 干跑 | 25/25 ✅ |
| Build | ✅ (9.11s) |
| WerewolfModule | 266.00 kB (+0.63 kB vs R136) |
| check-build | ✅（0 localhost/LAN 泄露） |

---

## 重要教训（→ LEARNINGS.md）

1. **R137-A 多狼协调铁律**：当多只狼人同时是警长候选时，后发言的狼人必须知道队友的已声明身份（从 claimHistory 读取）才能做出最优分工。**凡是给狼人新增竞选路径（悍跳新身份），必须同时检查多狼竞选场景是否需要协调更新（ssWolfCoordHint 的逻辑是否覆盖新路径）**。
2. **R137-B SS_WINDOW 升级铁律（再次确认）**：本轮 SS block 从 7520 增至 8894（+1374 chars），远超旧 SS_WINDOW 8500。升级至 9500，余量 606。**每次 SHERIFF_SPEECH 新增代码后必须运行：`node -e "const s=require('fs').readFileSync('src/services/aiPrompts.js','utf8'); const a=s.indexOf('case PROMPT_ACTIONS.SHERIFF_SPEECH:'); const end=s.indexOf('case PROMPT_ACTIONS.',a+1); console.log('block:',end-a,'余量:',9500-(end-a))"`。余量 < 500 时步进至 10500。**

---

## 下轮建议

**优先 A（需用户决策）**：平衡性调整（Options A/B/C pending，狼胜率 84-90% 仍高于目标 40-60%）

**优先 B（可直接执行）**：
- LAST_WORDS 骑士遗言补充：骑士死亡时如果 claimHistory 中有 jump_knight 悍跳者（非自己），遗言中可揭示对方是悍跳者（加大好人追查力度）
- 或多狼协调延伸：两只狼同时悍跳（都未跳时）的分工建议（谁应该跳谁应该走好人路线）
