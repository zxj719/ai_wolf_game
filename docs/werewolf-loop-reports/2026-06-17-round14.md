# 狼人杀评估优化报告 — 第 14 轮

> 时间：2026-06-17（第 14 次定时轮次）| 环境：云端定时任务

## 五行摘要

**本轮优化**：执行 `promptFactory.js` Plan B（延迟 13 轮的死代码清理）——删除 `getProgressiveActionPrompt` 函数、6 个主角色（werewolf/seer/witch/hunter/guard/villager）的 `daySpeech` 和 `nightAction` 函数、`dreamweaver#getDreamweaverNightPrompt`，共减少约 500 行死代码；同步清理 `CLAIMS_SCHEMA_SUFFIX` 孤立 import 和 8 处 `getBaseContext` 无用 import。  
**测试结果**：自定义 14/14 全通过；`npm run build + check-build` 通过（WerewolfModule-BofERG3e.js，无泄漏）；`node --check` 8 个修改文件均无语法错误。  
**关键教训**：死代码一旦形成就持续混淆开发者（14 轮里多次有人误改死代码然后困惑"为什么没效果"）；应在首次发现时立即删除，不要积压。  
**下轮计划**：① 狼人白天多狼分工提示（wolfTeammatesHint 细化）；② 实局 smoke test（14 轮无 LLM 验证）。  
**部署状态**：⚠️ 待部署（已提交到 main，无 CLOUDFLARE_API_TOKEN 跳过 wrangler deploy）

---

## 1. 客观评估（1-10 分）

| 维度 | 分数 | 说明 |
|------|------|------|
| 角色提示词质量 | 10/10 | 无新增提示词改动，维持 R13 水平 |
| 游戏触发机制正确性 | 9.5/10 | 维持 R13 水平 |
| AI 决策合理性 | 9.5/10 | 维持 R13 水平 |
| 代码可维护性 | 10/10 | 死代码清除，提示词路径唯一且可追踪 |
| 平衡性（狼/好人胜率） | 未测 | 14 轮均无真实 LLM 对局验证 |

---

## 2. 本轮优化详情

### 问题背景

自 Round 2 发现 `promptFactory.js#getProgressiveActionPrompt` 是死代码后，该清理任务被放入 `decisions-pending.md` 等待用户拍板。等待 13 轮无响应，LEARNINGS.md 已明确标注「下轮强制执行」。

### 删除范围

**死代码删除（~500 行）：**

| 文件 | 删除内容 |
|------|----------|
| `promptFactory.js` | `getProgressiveActionPrompt`（62 行）+ `CLAIMS_SCHEMA_SUFFIX` import + default export 条目 |
| `rolePrompts/werewolf.js` | `getWerewolfNightActionPrompt` + `getWerewolfDaySpeechPrompt`（63 行）|
| `rolePrompts/seer.js` | `getSeerNightActionPrompt` + `getSeerDaySpeechPrompt`（76 行）|
| `rolePrompts/witch.js` | `getWitchNightActionPrompt` + `getWitchDaySpeechPrompt`（75 行）|
| `rolePrompts/hunter.js` | `getHunterShootPrompt` + `getHunterDaySpeechPrompt`（53 行）|
| `rolePrompts/guard.js` | `getGuardNightActionPrompt` + `getGuardDaySpeechPrompt`（71 行）|
| `rolePrompts/villager.js` | `getVillagerDaySpeechPrompt`（39 行）|
| `rolePrompts/dreamweaver.js` | `getDreamweaverNightPrompt`（128 行，NIGHT_DREAMWEAVER case 有独立内联）|

**同步清理 import：**
- `werewolf.js`：`getBaseContext` 和 `isMiniGame`（后者长期未使用）
- `seer.js`：`getBaseContext`（`isLargeGame` 保留，用于 `getSeerThinkingDimensions`）
- `witch.js`：整行 `import { getBaseContext, isLargeGame }` 删除（两者均只在死代码中用）
- `hunter.js`：`getBaseContext`
- `guard.js`：`getBaseContext`
- `villager.js`：`getBaseContext`

**保留（活代码）：**
- `knight.js#getKnightDaySpeechPrompt` → 被 `ROLE_DAY_SPEECH_PROMPTS['骑士']` 调用
- `dreamweaver.js#getDreamweaverDaySpeechPrompt` → 被 `ROLE_DAY_SPEECH_PROMPTS['摄梦人']` 调用
- `magician.js#getMagicianDaySpeechPrompt` → 被 `ROLE_DAY_SPEECH_PROMPTS['魔术师']` 调用
- `magician.js#getMagicianNightActionPrompt` → 被 `NIGHT_MAGICIAN` case 直接调用
- 所有角色的 `buildPersonaPrompt` → 被 `aiPrompts.js#buildPersonaPrompt` 调用

### 验证方法

确认安全的核心步骤：
```bash
# 确认无外部引用
grep -rn "getWerewolfNightAction" src/ --include="*.js" | grep -v "rolePrompts/werewolf.js"
# → 0 results（只有自身文件的 export，无外部调用）
```

---

## 3. 测试结果

**脚本**：`.tmp/test-round14-plan-b.mjs`（14 项测试）

| 测试组 | 结果 |
|--------|------|
| T1-T5：死代码已删除（promptFactory + 6主角色 PROMPTS 对象无死字段） | ✅ 5/5 |
| T6-T10：活代码路径仍然存在（NIGHT_MAGICIAN/ROLE_DAY_SPEECH骑/梦/魔/knight/magician/dreamweaver） | ✅ 5/5 |
| T11-T13：所有9角色 buildPersonaPrompt + promptFactory 活导出 | ✅ 3/3 |
| T14：6个主角色中 getBaseContext import 已清理（dreamweaver 保留） | ✅ 1/1 |

**总计：14/14 passed**

`npm run build`：✅ 通过（check-build 无泄漏，WerewolfModule-BofERG3e.js）  
`node --check`：✅ 8 个修改文件全部通过

---

## 4. 评估综合打分趋势

| 维度 | R1 | R7 | R10 | R12 | R13 | R14 |
|------|----|----|----|-----|-----|-----|
| 角色提示词质量 | 6 | 9 | 10 | 10 | 10 | 10 |
| 触发机制正确性 | 7 | 8 | 9 | 9 | 9.5 | 9.5 |
| AI 决策合理性 | 5 | 9 | 9 | 9.5 | 9.5 | 9.5 |
| 代码可维护性 | 3 | 5 | 7 | 8 | 8 | 10 |

---

## 5. 下轮建议

1. **狼人白天多狼分工提示细化**（已在 LEARNINGS.md 条目 17）：
   - 在 `aiPrompts.js` 的 `ROLE_DAY_SPEECH_PROMPTS['狼人']` 函数中，`wolfTeammatesHint` 部分目前只说"分散发言立场"
   - 应增加具体分工建议：例如"2狼局建议一狼跳预言家搅浑水，一狼扮平民低调跟随"
   - 博弈论依据：Wang 2025 指出狼队"role differentiation"（角色分化）是多代理欺骗的关键成功要素

2. **实局 smoke test**（14 轮无真实 LLM 验证）：
   - 尝试检查 ECS 访问，对 `/api/werewolf/session/ask` 发单回合 payload
   - 重点验证：骑士决斗触发场景、魔术师 `shouldReveal` 字段实际产出

3. **hunterwolf daySpeech 多狼分工**（候选）：在多狼场景下，第一狼的 `multiWolfHint` 在白天应该包含"角色分化"策略，当前只有夜间协调，白天协调缺失。
