# Round 121 报告（2026-07-05）

## 优化目标

**女巫 DAY_VOTE 读写闭环**：从 `witchHistory.savedIds` 提取银水记录作为投票排除锚点，完成全 8 特殊角色的 DAY_VOTE 专属策略覆盖。

## 实施内容

### 核心改动：`src/services/aiPrompts.js`

在 DAY_VOTE case 的 `guardVoteStrategy` 声明之后、R71 语气风格注释之前，插入以下新代码（+约 1721 chars）：

1. **变量声明（角色门控）**
   - `dvWitchHistory`：`playerRole === '女巫' ? gameState.witchHistory : { savedIds: [], poisonedIds: [] }`
   - `dvWitchSavedAliveIds`：过滤存活银水玩家（savedIds ∩ alivePlayers）
   - `dvWitchPoisonedIds`：毒亡玩家列表（poisonedIds）

2. **摘要字符串**
   - `dvWitchSavedSummary`：有银水 → "X号（银水玩家，已确认盟友，从投票选项中排除）"；无 → "暂无存活银水"
   - `dvWitchPoisonedSummary`：有毒亡 → "X号（毒亡玩家）"；无 → "暂无"

3. **witchVoteStrategy（双路径）**
   - 有银水路径：🧪银水锚点 + 排除逻辑 + 三步投票优先排序 + 毒亡共谋推断（女巫独有）
   - 无银水路径：通用 fallback（有查杀 → 跟投查杀）

4. **三元链路由更新**：在守卫（R120）之后、通用 fallback 之前插入女巫专属路由

### 测试文件改动

| 文件 | 改动 | 原因 |
|------|------|------|
| `round121WitchDayVoteReadWriteClosure.test.js` | **新建** 20 个测试 | R121 正式验收 |
| `round72DayVoteDreamweaverMagician.test.js` | T14 `indexOf` → `lastIndexOf` | witch else-branch 引入同名文字，偏移伪造 |
| `round63KnightVoteDuelAlign.test.js` | T5 `indexOf` → `lastIndexOf` | 同上 |
| `round64SheriffBadgePassReadLoop.test.js` | T13 反转为正向确认 | witch 有专属策略，"有意缺失"测试变为过时 |
| `round118DayVoteKnightMagicianPrivate.test.js` | DV_WINDOW 16000 → 18000 | block 增长至 16786，ternary chain 超出 16000 |
| `round119SeerDayVoteReadWriteClosure.test.js` | DV_WINDOW → 18000（已在前轮执行） | 同上 |
| `round120GuardDayVoteReadWriteClosure.test.js` | DV_WINDOW → 18000（已在前轮执行） | 同上 |
| `round71DayVotePersonalityStyle.test.js` | DV_WINDOW → 18000（已在前轮执行） | 同上 |
| `aiPrompts.js`（注释修复） | 将注释 `无"绝不投/禁止"等负向禁令` → `无负向禁令词汇` | 注释中的禁词触发 R120 白熊测试 |

## 数据指标

| 指标 | 数值 |
|------|------|
| DAY_VOTE block 大小 | 15065 → 16786 chars（+1721） |
| `输出JSON格式:` 偏移 | 14825 → 16546（仍在 18000 窗口内） |
| 新增测试 | 20（R121 测试文件） |
| 修复测试 | 4（R63 T5、R72 T14、R64 T13、R118 T12） |
| 通过测试总数 | 2214/2214（chatSocket 基础设施失败不计入） |
| 构建状态 | ✅ green |
| 静态分析 | ✅ 0 泄漏 |

## 设计亮点

**毒亡共谋推断**是女巫独有的 DAY_VOTE 推理路径：若毒亡玩家生前曾为某存活玩家"金水/力挺"，该存活玩家的共谋嫌疑上升。这利用了女巫的全知状态（她是唯一知道谁被自己毒死的人）来构建 DAY_VOTE 的额外推理层。

## R121 完成里程碑：DAY_VOTE 全角色覆盖

| 角色 | 完成轮次 | 核心信息锚点 |
|------|---------|------------|
| 狼人 | R61 | 警方压力分析 |
| 猎人 | R62 | 开枪优先级 |
| 骑士 | R63 / R118 | 决斗候选 / 已决斗出局 |
| 摄梦人 | R72 | 连梦候选（identity_table） |
| 魔术师 | R72 / R118 | 换刀候选 / 交换验证确认 |
| 预言家 | R119 | seerChecks（三路径） |
| 守卫 | R120 | guardHistory（守护锚点） |
| **女巫** | **R121** | **witchHistory（银水/毒亡）** |

## 决策追踪

- 10p/3w 平衡问题：仍在 `werewolf-decisions-pending.md`，等待用户输入（wolf win rate 84-90%）
- SHERIFF_BADGE_PASS 全角色审计：推迟至 R122+

## 下轮建议（R122）

**优先级 A**：实施 10p/3w 平衡调整（需用户决策，已三轮推迟）
- Option A：增加特殊角色密度（村民→神职）
- Option B：减小 AI 投票噪声（`Math.random() * 2.5` → `1.5`）
- Option C：预言家查验频率 40% → 50%

**优先级 B**：SHERIFF_BADGE_PASS 全角色审计（女巫/预言家在 BADGE_PASS 是否有专属判断？）

