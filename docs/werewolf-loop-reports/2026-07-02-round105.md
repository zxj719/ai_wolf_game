## Round 105 — 2026-07-02

> ⚠ 待部署：本轮改动已提交未上线（无 CLOUDFLARE_API_TOKEN）

### 五行总结

本轮完成**女巫 DAY_SPEECH 三连平安夜三阶推断**（isTripleConsecutivePeacefulWitch），女巫 DAY 三层体系闭合（R82 单夜→R92 两连→R105 三连）。测试 1804/1804 ✅，干跑 17/17 ✅，build ✅（WerewolfModule 242.61 kB）。新增教训：固定窗口测试中变量声明和函数体 trim 的切片逻辑须精确匹配代码实际结构（见 R105-B）。下轮建议：补充 DAY_SPEECH 三连推断中尚缺的角色，或转向 NIGHT 侧女巫（isConsecutivePeacefulNightWitch + 三连）。

---

### 评估评分

| 维度 | 分 | 说明 |
|------|-----|------|
| 推断深度 | 8/10 | 女巫 DAY 三层完整；其余 NIGHT 侧女巫尚缺两/三连 |
| 个性差异化 | 8/10 | 女巫 7 种个性 + 发言字数差异化已覆盖 |
| 话术一致性 | 9/10 | 白熊效应第 26 次验证全通过 |
| 测试覆盖 | 9/10 | 1804/1804；round58/70 窗口级联更新规范化 |
| 推断精确性 | 7/10 | 女巫依赖票压代理（非 guardHistory 零间接），精度层级低于守卫 |

**选定优化项：女巫 DAY_SPEECH 三连平安夜推断（isTripleConsecutivePeacefulWitch）**

理由：上轮（R104）守卫三连完成，按系列顺序，女巫 DAY 是唯一缺三连的神职角色。女巫有 hasWitchSave 状态双轨，三连推断能给出"守卫三夜锁守"的最高置信度结论，具备独特信息价值。

---

### 本轮实现

**女巫 DAY_SPEECH 三连平安夜三阶推断（Prepend Injection 第 20 次）**

#### 编辑 1：外层变量（isTripleConsecutivePeacefulWitch）

在 `isConsecutivePeacefulWitch` 声明之后新增：

```javascript
// R105：三连平安夜检测（D4+，守卫连守三夜极高概率）
const isTripleConsecutivePeacefulWitch = ctx.dayCount >= 4 && isConsecutivePeacefulWitch &&
    ctx.fullGameTimeline?.includes(`N${ctx.dayCount - 3}:平安夜`);
```

#### 编辑 2：if 块内新变量（tripleConsecutivePeaceHintWitch）

在 `if (isPeacefulNightWitch)` 内 `prevPrevDay` 之后：

```javascript
// R105：三连推断变量（零推断——依赖票压代理，无 guardHistory 直读）
const threePrevDay = ctx.dayCount >= 4 ? ctx.dayCount - 3 : 0;
const tripleConsecutivePeaceHintWitch = isTripleConsecutivePeacefulWitch
    ? `⭕【女巫三连平安夜三阶推断（N${threePrevDay}+N${prevPrevDay}+N${prevDay}均无死亡）】
- 对比 D${threePrevDay}、D${prevPrevDay}、D${prevDay} 高票存活者：
  - 路径A（三夜相同）：守卫三夜锁守该目标，confidence 升 35-45；解药待①②双信号出手
  - 路径B（两夜相同+一夜不同）：守卫高频连守，该高频目标 confidence 升 30-40；另一夜独立单夜评估
  - 路径C（三夜各不同）：守卫轮守无固定模式，按单夜路径A/B独立评估
- identity_table 追加"D${threePrevDay}-D${prevPrevDay}-D${prevDay}三连平安夜：[路径A锁守/路径B高频/路径C轮守]，confidence 升 35-45/30-40/按单夜"\n`
    : '';
```

#### 编辑 3：Prepend Injection（第 20 次）

```javascript
// BEFORE:
const consecutivePeaceHintWitch = isConsecutivePeacefulWitch
    ? `⭕【女巫连续两夜平安夜二阶推断...

// AFTER:
const consecutivePeaceHintWitch = isConsecutivePeacefulWitch
    ? `${tripleConsecutivePeaceHintWitch}⭕【女巫连续两夜平安夜二阶推断...
```

---

### 女巫 DAY 三层体系完整性

| 层级 | 变量 | 条件 | confidence 增量 |
|------|------|------|----------------|
| 单夜（R82）| `isPeacefulNightWitch` | D2+，昨夜平安 | 解药联动 |
| 两连（R92）| `isConsecutivePeacefulWitch` | D3+，N-2 也平安 | 升 25-35（路径A）/ 降 10-15（路径B） |
| 三连（R105）| `isTripleConsecutivePeacefulWitch` | D4+，N-3 也平安 | 升 35-45（路径A）/ 升 30-40（路径B） |

女巫精度层级：票压代理（非直接 guardHistory），低于守卫（零间接），高于村民（纯票压）。三连激活时 hasWitchSave 信息与票压交叉验证给出最强置信度。

---

### 级联测试窗口更新

witch block 4523 → 5414 chars（+891 chars），`${witchSpeechLen}` 从 5742 移至 6633：

| 文件 | 变更 | 原因 |
|------|------|------|
| `round58WitchGuardDaySpeechStep0.test.js` | witch window 6000→7500 | `${witchSpeechLen}` 在 6633 超出旧窗口 |
| `round70SpeechLenDifferentiation.test.js` | witch window 6500→7500 | 同上 |
| `round82WitchPeaceNightInference.test.js` | 无需更新（window 10000） | — |
| `round92WitchConsecutivePeaceNight.test.js` | 无需更新（window 12000） | — |
| `round95WitchAntidotePathARefine.test.js` | 无需更新（window 12000） | — |
| `round68DivineRolePersonalityLens.test.js` | 无需更新（动态边界 '猎人'） | — |

---

### 新测试（round105WitchDayTripleConsecutivePeace.test.js）

20 个测试 T1-T20：
- T1-T4：`isTripleConsecutivePeacefulWitch` 外层变量（D4+、依赖 isConsecutive、fullGameTimeline N-3）
- T5-T7：if 块内变量（threePrevDay、tripleConsecutivePeaceHintWitch 三元、空串 fallback）
- T8-T13：内容验证（三连标头、路径A/B/C、confidence 35-45/30-40、identity_table）
- T14：白熊效应合规（第 26 次验证，无负向禁词）
- T15-T17：Prepend Injection 结构（${tripleConsecutivePeaceHintWitch} 存在、prepend 顺序、超集激活原则）
- T18-T19：声明顺序（三连在两连之后、R92 回归保留）
- T20：block 大小 6500-10000

---

### 质量验证

- **测试**：1804/1804 ✅（+20 new R105 tests；+round58/70 窗口级联更新；1 pre-existing chatSocket 失败与本轮无关）
- **构建**：WerewolfModule 242.61 kB ✅
- **check-build**：无 localhost/127.0.0.1 泄露 ✅
- **白熊效应**：第 26 次验证 ✅（T14 覆盖）
- **Prepend Injection**：第 20 次应用 ✅
- **干跑**：17/17 ✅

---

### 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/services/aiPrompts.js` | 修改 | 女巫三连推断三处编辑（+891 chars） |
| `src/services/__tests__/round105WitchDayTripleConsecutivePeace.test.js` | 新建 | 20 个测试 |
| `src/services/__tests__/round58WitchGuardDaySpeechStep0.test.js` | 修改 | witch window 6000→7500 |
| `src/services/__tests__/round70SpeechLenDifferentiation.test.js` | 修改 | witch window 6500→7500 |

---

### 下轮优先项

1. **女巫 NIGHT 侧两连+三连推断**（NIGHT_WITCH case，参考 R102 狼人 NIGHT 一轮完成两连+三连模式）
2. **预言家/村民已有四层（DAY+NIGHT×2），守卫 DAY 三层完整**——女巫 NIGHT 两/三连是最后缺口
3. 可选：新角色平安夜推断（骑士 NIGHT 侧，若有 NIGHT_KNIGHT case）
