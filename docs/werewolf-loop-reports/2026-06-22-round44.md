# 狼人杀评估优化报告 — 第 44 轮

> 时间：2026-06-22（第 44 次定时轮次）| 环境：云端定时任务

## 五行摘要

**本轮优化**：骑士 identity_table DAY→DAY 读写闭环——补全 `getKnightDaySpeechPrompt` 的 `knightHistoryStep`（Step 0）和"决斗候选"写指导。骑士是本游戏唯一"白天技能"角色，其决斗候选置信度应跨天积累而非每天从头分析；R43 已完成全 6 个夜间行动角色的 DAY→NIGHT 和 NIGHT→NIGHT 闭环，骑士的 DAY→DAY 是最后一个独立缺口。  
**测试结果**：34/34 自定义测试通过；`npm run build` 通过（WerewolfModule-BxsEQJ31.js，213.64kB，+0.55kB，check-build 无泄漏）；`node --check` 通过；vitest 479/479 passed（1 pre-existing ws 缺失失败，非本轮引入）。  
**新增教训**：骑士的 DAY→DAY 闭环是"读写模式"扩展到白天技能角色的首例——与 NIGHT→NIGHT 和 DAY→NIGHT 共同构成三类闭环；常驻诊断命令对无"下轮"关键词的缺口无效，需要人工评估有白天技能的特殊角色。  
**下轮计划**：完成全角色 identity_table 读写闭环体系（三类：NIGHT→NIGHT、DAY→NIGHT、DAY→DAY）；下轮可做效果验证 smoke test，或评估是否有其他跨白天积累需求的角色（目前骑士是唯一案例）。

---

## ⚠ 待部署：本轮改动已提交未上线

（无 CLOUDFLARE_API_TOKEN 环境变量，改动已 push 到 main，等待用户手动部署或 CI 触发）

---

## 1. 客观评估（1-10 分）

| 维度 | 分数 | 说明 |
|------|------|------|
| 角色提示词质量 | 10/10 | 三类 identity_table 读写闭环全部完成（NIGHT→NIGHT：wolf/witch/guard/seer/dreamweaver/magician；DAY→NIGHT：wolf/seer/witch/guard/dreamweaver/magician；DAY→DAY：骑士） |
| 游戏触发机制正确性 | 10/10 | 无触发机制改动 |
| AI 决策合理性 | 9/10 | 骑士第 D2+ 天决斗时可利用历史积累的候选目标置信度，不再重新评估 |
| 代码可维护性 | 10/10 | 遵循 R38-R43 规范：`ctx.dayCount > 1` 条件分支 + 追加示例 + 非硬约束声明 |
| 平衡性（狼/好人胜率） | 未测 | 44 轮累计 0 次真实 LLM 对局验证 |

---

## 2. 问题诊断（骑士 DAY→DAY 闭环评估）

### 缺口分析

骑士的 `getKnightDaySpeechPrompt`（`knight.js`）在 R44 之前：
- ✗ **无 Step 0**：思维链直接从 Step1（场上局势分析）开始，每天从头推断，无历史记忆
- ✗ **无"决斗候选"写指导**：`identity_table` 输出 schema 只有通用 `reason` 字段，AI 不知道该写什么关键词
- ✗ **无追加指令**：没有"追加不覆盖历史"指令和格式示例

### 为什么骑士需要 DAY→DAY 而非 DAY→NIGHT

骑士（和其他角色不同）的关键技能是白天决斗，没有夜间行动：
- 其他角色：白天积累判断 → 当晚行动（DAY→NIGHT）
- 骑士：白天积累判断 → **次天**行动（DAY→DAY）

这是读写模式在游戏中的第三种变体，之前的闭环体系并不覆盖它。

### 博弈论依据

Wang 2025 (arxiv:2408.17177) §4.3 指出：多代理博弈中，"信念更新"（belief update）是关键组件——每个 Agent 应该有机制追踪其他玩家身份信念的演变，而不是每轮从零建立。骑士的决斗是"一次性验证"行动，错误成本极高（误伤好人），因此其信念积累尤为重要——D1 的"40%嫌疑"加上 D2 的"新发言漏洞"应该叠加到"70%+ 可决斗"，而不是 D2 重新从 0 开始。

---

## 3. 技术实现

### 修改文件：`src/services/rolePrompts/knight.js`

#### 修改 1：新增 `knightHistoryStep` 变量（+12 行）

```diff
+ // R44 DAY→DAY 读写闭环：历史决斗候选读取步骤
+ const knightHistoryStep = ctx.dayCount > 1
+   ? `0. 【读取历史决斗候选】查看系统提示中【你之前的身份推理表】：哪些玩家的 reason 含"决斗候选"字样？
+      - 确认这些候选目标是否仍然存活（已出局则跳过）
+      - 结合今天新信息（发言漏洞/死亡验证/验人结果）更新候选优先级
+      - 历史候选是决策起点，不是硬约束——若新信息推翻旧候选，更新并在 thought 中说明原因`
+   : '0. 【第一天】无历史决斗候选记录——直接根据当前局势推断决斗候选目标';
```

#### 修改 2：思维链注入 `${knightHistoryStep}` 作为 Step 0

```diff
  【思维链（必须完成）】
+ ${knightHistoryStep}
  Step1: 场上局势分析
```

#### 修改 3：`输出JSON` 前添加 identity_table 写指导（+9 行）

```diff
+ 【identity_table 填写指导（骑士白天：决斗候选积累 DAY→DAY 闭环）】
+ - 高嫌疑决斗目标：reason 写"决斗候选：[优先级A/B/C]，[逻辑依据摘要]"（下天 Step 0 将直接从此读取）
+   【追加示例】上轮 reason="发言可疑" → 本轮追加为"发言可疑；决斗候选：优先级A，预言家对跳逻辑链崩溃"
+ - 确认好人/真神职：reason 写"铁好人：[依据]；禁止决斗"（防止误伤）
+ - 行为中立玩家：正常记录推断，不加"决斗候选"标签
+ - 已决斗出局目标：reason 追加"→已决斗出局"
+ - 每轮在上轮 reason 基础上追加本轮新观察，不要覆盖历史
```

#### 修改 4：输出 JSON `thought` 字段提示更新

```diff
- "thought":"完整的5步思维链"
+ "thought":"完整的6步思维链（含Step0读历史）"
```

---

## 4. 完整 identity_table 读写闭环状态（三类全覆盖）

### NIGHT→NIGHT 闭环（跨夜读写）

| 角色 | NIGHT_* Step 0 读关键词 | 完成轮次 |
|------|----------------------|---------|
| 狼人 | `高优先刀口` | R38 |
| 女巫 | `毒药优先候选` | R39 |
| 守卫 | `守护优先级：高` | R40 |
| 预言家 | `排队查验优先级` | R41 |
| 猎人 | `开枪优先级：高` | R41 |
| 摄梦人 | `连梦候选` | R43 |
| 魔术师 | `换刀候选`/`保核目标候选` | R43 |

### DAY→NIGHT 闭环（白天写→当夜读）

| 角色 | DAY_SPEECH 写关键词 | NIGHT_* Step 0 读关键词 | 完成轮次 |
|------|-------------------|----------------------|---------|
| 狼人 | `高优先刀口：...` | `高优先刀口` | R42 |
| 预言家 | `排队查验优先级：①②③④⑤之一` | `排队查验优先级` | R42 |
| 女巫 | `毒药优先候选：...` | `毒药优先候选` | R42 |
| 守卫 | `守护优先级：高` | `守护优先级：高` | R42 |
| 摄梦人 | `连梦候选：...` | `连梦候选` | R43 |
| 魔术师 | `换刀候选：...`/`保核目标候选：...` | `换刀候选`/`保核目标候选` | R43 |

### DAY→DAY 闭环（跨白天读写，骑士专属）

| 角色 | DAY_SPEECH 写关键词 | 次天 Step 0 读关键词 | 完成轮次 |
|------|-------------------|-------------------|---------|
| 骑士 | `决斗候选：[优先级A/B/C]` | `含"决斗候选"字样` | **R44 本轮** |

---

## 5. 测试结果

**脚本**：`.tmp/test-round44.mjs`（34 项测试）

| 测试组 | 结果 |
|--------|------|
| T1-T8：knightHistoryStep 变量声明 8 项 | ✅ 8/8 |
| T9-T14：思维链 Step 0 注入 6 项 | ✅ 6/6 |
| T15-T22：identity_table 写指导 8 项 | ✅ 8/8 |
| T23-T28：格式与数量完整性 6 项 | ✅ 6/6 |
| T29-T32：回归——原有骑士功能未破坏 4 项 | ✅ 4/4 |
| T33-T34：常驻诊断 2 项 | ✅ 2/2 |

**总计：34/34 passed**

`npm run build`：✅ 通过（WerewolfModule-BxsEQJ31.js，213.64kB，check-build 无泄漏）  
`node --check`：✅ knight.js 通过  
`vitest`：✅ 479/479 passed（1 pre-existing ws 缺失失败，非本轮引入）

---

## 6. smoke test 状态

ECS 不可达（44 轮累计 0 次真实 LLM 验证）。

建议用户本地验证（本轮重点）：
1. **骑士决斗候选连续性**：若 D1 发言标注 X 号"决斗候选：优先级A，悍跳预言家逻辑链崩溃"，D2 的 `thought` 是否出现"读取历史决斗候选：X号"等 Step 0 框架语言？
2. **骑士候选追加格式**：连续 2 天后，identity_table 的 X 号 reason 是否出现"D1 观察；决斗候选：..."的追加格式（而非覆盖）？

---

## 7. SHERIFF_SPEECH 常驻检查

```bash
grep -o "playerRole === '[^']*'" src/services/aiPrompts.js | sort -u
```

覆盖状态：预言家/狼人/猎人/摄梦人/魔术师/骑士/女巫/守卫（全角色，R32 里程碑）— 无缺口。

---

## 8. 下轮建议（优先级排序）

1. **实局 smoke test**（最高优先级，44 轮未完成）：ECS 不在云端 allowlist；建议用户本地验证骑士 DAY→DAY 连续性（参见第 6 节验证步骤）
2. **三类闭环全景回验**（新增）：用户本地验证 DAY→NIGHT（白天"高优先刀口/毒药优先候选/守护优先级：高/排队查验优先级"能否被次夜 Step 0 正确召回）+ DAY→DAY（骑士"决斗候选"能否被次天 Step 0 召回）
3. **常驻诊断**：每轮结束后运行 `grep -n "供下轮\|下轮用\|下轮参考\|下轮复查" src/services/aiPrompts.js src/services/rolePrompts/*.js`，确认无悬空前向引用
4. **DAY_VOTE 热力盲从效果回验**（item 80 续）：用户本地验证 Step A 三维评分语言是否出现在 thought 字段
