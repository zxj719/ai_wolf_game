# Round 130 — bpHunterHint 枪×徽协同结构化数据注入

⚠ 待部署：本轮改动已提交推送至 main，但无 CLOUDFLARE_API_TOKEN，未执行 `npm run deploy`。

**日期**：2026-07-07  
**耗时**：~20 min

---

## 本轮摘要（5 行）

- **优化**：新增 `bpHunterHint` — 猎人 SHERIFF_BADGE_PASS 专属结构化数据注入（枪×徽协同）
- **改动文件**：`src/services/aiPrompts.js`（+14行 bpHunterHint 块 + 模板注入）
- **新增测试**：`round130HunterBadgePassSeerDataInjection.test.js`（14 项，全部通过）
- **测试**：2352/2352 通过（+14 新增），chatSocket.integration 预存失败（ws 包缺失，无关）
- **下轮建议**：⚠ BP_WINDOW 余量仅 348 chars（安全线 300），预防性升级 10000 → 12000

---

## 五维评估结论

| 维度 | 状态 | 备注 |
|------|------|------|
| 提示词质量 | ✅ | 猎人 BADGE_PASS 从「文字指导」升级为「结构化数据注入」|
| 信息注入 | ✅ | bpHunterHint 复用已计算的 killedTargets/goldWaterTargets |
| 平衡性 | ⚠ 待确认 | 狼胜率 84-90%；Option A/B/C 等待用户拍板 |
| 提示词合规 | ✅ | bpHunterHint 块无「不要/禁止/绝不能/千万别」负向禁令词 |
| 测试覆盖 | ✅ | 2352/2352 通过，T12 BP_WINDOW 哨兵确认余量 348 chars |

---

## 优化目标（本轮：R130）

**bpHunterHint — 猎人 BADGE_PASS 结构化数据注入**

### 问题识别（R127/R129 遗留）

R127 为猎人添加了 `bpHint` + `bpIdentityStep` 专属文本，实现了「枪×徽协同」的**文字描述**。  
但女巫/守卫/摄梦人均有私有数据块（`bpWitchHint`/`bpGuardHint`/`bpDreamweaverHint`），  
将游戏历史过滤到 `badgeableSet`，生成「候选中存活的优先/回避列表」。  
猎人缺少同等的结构化注入，「目标分离」原则只停留在文字层面。

R129「下轮建议优先B」明确提出：
> 验证猎人枪击×传徽协同在实际对局中的表现（BADGE_PASS thought 中目标分离）

### 博弈论依据

Wang 2025 (arxiv:2408.17177) 研究表明：角色专属结构化私有信息注入显著优于泛化文字描述。  
`killedTargets`（查杀候选在 badgeableSet 中）和 `goldWaterTargets`（金水候选在 badgeableSet 中）  
已在 BADGE_PASS 块顶部计算，零额外数据读取即可直接复用。

---

## 实现细节

### 修改文件 1：`src/services/aiPrompts.js`

新增 `bpHunterHint` 块（在 `bpDreamweaverHint` 之后，`bpIdentityStep` 之前）：

```javascript
// R130: 猎人 SHERIFF_BADGE_PASS 专属提示词 — 枪击×传徽目标分离（结构化数据注入）
// killedTargets/goldWaterTargets 已在上方计算（seerChecks 过滤 badgeableSet）
let bpHunterHint = '';
if (playerRole === '猎人') {
    const wolfCandidates = killedTargets.map(c => `${c.targetId}号`);
    const goldCandidates = goldWaterTargets.map(c => `${c.targetId}号`);
    if (wolfCandidates.length > 0) {
        bpHunterHint += `\n🔫【猎人枪×徽协同 — 回避传徽】候选中含查杀目标 ${wolfCandidates.join('、')} → 这是出局时枪击首选；传徽给同一人等于两张牌合并打同一目标、协同价值归零。`;
    }
    if (goldCandidates.length > 0) {
        bpHunterHint += `\n🔫【猎人枪×徽协同 — 优先传徽】候选中含金水好人 ${goldCandidates.join('、')} → 传徽首选（枪打好人自废枪权，传给好人让1.5票持续发挥）。`;
    }
}
```

模板注入（return 语句，在 `${bpDreamweaverHint}` 之后追加）：

```diff
- ${bpIdentityStep ? bpIdentityStep + '\n' : ''}${bpHint}${seerHint}${bpWitchHint}${bpGuardHint}${bpKnightHint}${bpMagicianHint}${bpDreamweaverHint}
+ ${bpIdentityStep ? bpIdentityStep + '\n' : ''}${bpHint}${seerHint}${bpWitchHint}${bpGuardHint}${bpKnightHint}${bpMagicianHint}${bpDreamweaverHint}${bpHunterHint}
```

### 修改文件 2：`src/services/__tests__/round130HunterBadgePassSeerDataInjection.test.js`（新建）

14 项测试，覆盖：
- T1-T2: 锚点 + R130 版本标识
- T3-T6: bpHunterHint 变量声明与「回避/优先传徽」关键词
- T7-T8: killedTargets / goldWaterTargets 引用
- T9: `${bpHunterHint}` 模板注入存在
- T10: 注入顺序（bpDreamweaverHint 在前，bpHunterHint 在后）
- T11: 白熊效应合规
- T12: BP_WINDOW 哨兵（输出JSON偏移量 < 10000）
- T13-T14: 回归（R123/R127 已有内容完整）

---

## 窗口影响分析

| 窗口 | R129 后 | R130 后 | 余量 |
|------|---------|---------|------|
| BP_WINDOW | ~8751 chars | 9652 chars | 348 chars（≥300 安全线 ✅，但接近预警） |
| LW_WINDOW | 5286 chars | 5286 chars（未改动） | 1714 chars（within 7000 ✅） |
| WerewolfModule bundle | 262.19 kB | 262.49 kB | +0.30 kB（预期净增量） |

---

## 测试结果

| 指标 | 值 |
|------|----|
| 总测试数 | 2352/2352 ✅ |
| 新增测试 | 14（round130 全部通过） |
| 回归修复 | 无 |
| 预存失败 | 1（chatSocket.integration，ws 包缺失，无关） |
| Build | ✅ |
| check-build | ✅ 0 localhost 泄露 |
| WerewolfModule | 262.49 kB（+0.30 kB，符合预期） |
| BP_WINDOW 余量 | 348 chars（JSON offset=9570 < 10000 ✅） |

---

## 重要教训（→ LEARNINGS.md）

1. **R130-A**：猎人 BADGE_PASS 结构化数据注入 — 复用已计算的 `killedTargets`/`goldWaterTargets`（badgeableSet 过滤），零额外 gameState 读取。私有数据块格式：`🔫【猎人枪×徽协同 — 回避/优先传徽】`
2. **R130-B**：BP_WINDOW 余量预警 — R130 后 BADGE_PASS 块为 9652 chars，余量仅 348 chars（安全线 300）。下轮若有任何 BADGE_PASS 修改，须先将 BP_WINDOW = 10000 → 12000

---

## 下轮建议

**优先 A（必做，基础设施维护）**：BP_WINDOW 预防性升级
- BADGE_PASS 块已达 9652 chars，余量 348 chars 接近 300 chars 安全线
- 升级 `BP_WINDOW = 10000 → 12000`（两个测试文件：round113、round127 及相关测试）

**优先 B（需用户决策）**：平衡性调整
- docs/werewolf-decisions-pending.md Option A/B/C 等待用户拍板

**优先 C（可直接执行）**：
- BADGE_PASS 所有分支实战验证（R130 新增 bpHunterHint 后的对局表现）
- 考察 DAY_SPEECH 专属分支现状（对标 DAY_VOTE/BADGE_PASS 的 8 角色完整覆盖）
