# Round 123 报告（2026-07-05）

## 5 行摘要

- **本轮优化**：为 `SHERIFF_BADGE_PASS` 补全摄梦人专属分支（`bpDreamweaverHint` + `bpIdentityStep` 第 8 路径 + `bpHint` 第 8 路径），至此全 8 神职均有专属传徽策略。
- **测试结果**：2254/2254 通过（+15 新增 R123 测试）；干跑 25/25 ✅；构建 ✅ 静态分析 ✅ 0 泄漏。
- **新增教训**：BP_WINDOW 逼近上限时（≤ 173 chars 余量），需同批次将所有 BADGE_PASS 测试文件的 BP_WINDOW 升级（本轮 8500→10000）；SHERIFF_VOTE→LAST_WORDS 联合块大小哨兵（R115 T20）在 BADGE_PASS 有意增长时需同步更新。
- **下轮计划**：10p/3w 平衡调整（已等待 3+ 轮用户决策，Option A/B/C 见 `werewolf-decisions-pending.md`），或猎人 SHERIFF_BADGE_PASS 专属分支（价值较低，因无专有历史记录）。
- **部署状态**：⚠ 待部署：改动已 push 到 main，无 CLOUDFLARE_API_TOKEN，未执行 `npm run deploy`。

---

## 优化目标

**摄梦人 SHERIFF_BADGE_PASS 专属分支**：SHERIFF_BADGE_PASS 已有 狼人/女巫/守卫/骑士/魔术师/预言家 六个专属分支，但摄梦人走通用"好人警长" fallback，未利用 `dreamweaverHistory.dreamedPlayers`（入梦历史）这一零间接信任锚点。

## SHERIFF_BADGE_PASS 全角色覆盖状态（R123 完成后）

| 角色 | 完成轮次 | 核心私有信息锚点 |
|------|---------|----------------|
| 狼人 | R64（初始） | 传队友/混淆策略 |
| 女巫 | R113 | witchHistory.savedIds（银水） |
| 守卫 | R113 | guardHistory（守护频次） |
| 骑士 | R117 | hasUsedDuel（能力状态） |
| 魔术师 | R117 | hasRevealed + magicianHistory |
| 预言家 | R122 | seerChecks（一手验证 > 推断） |
| **摄梦人** | **R123** | **dreamedPlayers（入梦频次 > identity_table 推断）** |
| 猎人 | 待定（R124+） | — |
| 村民 | 通用 fallback（有意设计）| — |

## 实施内容

### 核心改动：`src/services/aiPrompts.js`

**1. `bpDreamweaverHint` 新增专属变量块**

在魔术师块（`bpMagicianHint`）之后、`// R64 读写闭环` 注释之前插入：

```javascript
// R123: 摄梦人 SHERIFF_BADGE_PASS 专属提示词 — 入梦历史作信任锚点
const bpDreamweaverHistory = gameState.dreamweaverHistory || { dreamedPlayers: [], lastDreamTarget: null };
const bpDreamweaverDreamed = bpDreamweaverHistory.dreamedPlayers || [];
let bpDreamweaverHint = '';
if (playerRole === '摄梦人' && bpDreamweaverDreamed.length > 0) {
    const dwDreamCounts = {};
    bpDreamweaverDreamed.forEach(id => {
        if (id != null && badgeableSet.has(id)) {
            dwDreamCounts[id] = (dwDreamCounts[id] || 0) + 1;
        }
    });
    const topDreamed = Object.entries(dwDreamCounts)
        .sort((a, b) => b[1] - a[1])
        .slice(0, 2)
        .map(([id, cnt]) => `${id}号(入梦${cnt}次)`);
    if (topDreamed.length > 0) {
        bpDreamweaverHint = `\n🌙【你的入梦记录（存活候选中入梦最频繁者）】${topDreamed.join('、')} → 你最愿意为之承担同生共死风险的好人，优先传徽`;
    }
}
```

**2. `bpIdentityStep` 新增第 8 路径（摄梦人分支）**

在 `playerRole === '预言家'` 分支之后、`others` fallback 之前插入：
```
: playerRole === '摄梦人'
? 'Step0: 查看【你之前的身份推理表】作为传徽补充参考（confidence ≥ 70 且 suspect 不含"狼人"的存活候选人）。\nStep1: 传徽优先级 → ⚡预言家金水 > 🌙入梦最频繁者（你愿为之同生共死）> identity_table ≥70 非狼嫌疑 > 发言可信者 > -1撕毁。'
```

**3. `bpHint` 新增第 8 路径（摄梦人分支）**

在 `playerRole === '预言家'` 分支之后、`好人警长` fallback 之前插入：
```
: playerRole === '摄梦人'
? '你是摄梦人警长：入梦记录是你最信任的好人信号（你愿为之承担同生共死风险）——参考入梦历史中存活的候选人和预言家金水，选出最可信的传徽对象；无充分依据时撕掉（-1）。'
```

**4. return 模板追加 `${bpDreamweaverHint}`（空串时零副作用）**

### 测试文件改动

| 文件 | 操作 | 说明 |
|------|------|------|
| `round123DreamweaverBadgePassBranch.test.js` | 新建 15 个测试 | R123 正式验收 |
| `round113SheriffBadgePassWitchGuard.test.js` | T12 窗口 1200→1600；BP_WINDOW 8500→10000 | R123 摄梦人 bpIdentityStep 分支将 others fallback 推至 offset ~1377，超出旧 1200 |
| `round115SheriffVoteDreamweaver.test.js` | T20 上限 15000→17000 | SHERIFF_VOTE→LAST_WORDS 联合块（含 BADGE_PASS）随 R123 +1525 chars 增至 15623 |
| `round117SheriffBadgePassKnightMagician.test.js` | BP_WINDOW 8500→10000 | 预防性升级 |
| `round122SeerBadgePassBranch.test.js` | BP_WINDOW 8500→10000 | 预防性升级 |

## 数据指标

| 指标 | 数值 |
|------|------|
| BADGE_PASS block 大小 | 6802 → 8327 chars（+1525） |
| BP_WINDOW（所有 BADGE_PASS 测试） | 8500 → 10000 |
| R115 T20 上限 | 15000 → 17000 |
| 新增测试 | 15（R123 测试文件）|
| 修复测试 | 4（R113 T12 + R115 T20 哨兵 + R117/R122 BP_WINDOW 升级） |
| 通过测试总数 | 2254/2254（chatSocket 基础设施失败不计入） |
| 构建状态 | ✅ green（WerewolfModule 258.59→260.19 kB, +1.6 kB） |
| 静态分析 | ✅ 0 泄漏 |
| 干跑 | 25/25 ✅ |

## 设计依据

1. **Wang 2025 (arxiv:2408.17177)**：角色私有信息注入显著提升 AI 决策质量。
2. **摄梦人入梦的信任语义**：`dreamedPlayers` 是摄梦人主动选择承受"同生共死"风险的目标列表。多次入梦同一玩家 = AI 表达信任的行为记录，与守卫守护频次（R113）在认识论层次等价：**直接行为记录（ground truth）> identity_table NLP 推断**。
3. **正向描述铁律（R1/R45）**：全部使用"你最愿意为之承担同生共死风险的好人，优先传徽"而非"不传给未入梦过的人"。

## 决策追踪

- 10p/3w 平衡问题：仍在 `werewolf-decisions-pending.md`，等待用户输入（wolf win rate 84-90%）
- 猎人 BADGE_PASS：猎人无特殊私有历史记录，分支价值较低，暂推后

## 下轮建议（R124）

**优先级 A**：实施 10p/3w 平衡调整（已 4+ 轮推迟，需用户决策）
- Option A：增加神职比例（村民→神职）
- Option B：减小 AI 投票噪声（`Math.random() * 2.5` → `1.5`）
- Option C：预言家查验命中率 40% → 50%

**优先级 B**：猎人 SHERIFF_BADGE_PASS 专属分支（价值低，hunter 无专有历史记录，可能跳过）

**优先级 C**：平衡性评估（N=100 局干跑模拟统计狼/好人胜率，`.tmp/balance-sim.mjs`）
